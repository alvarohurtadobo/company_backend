from django.apps import AppConfig


class WoodConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "wood"
    verbose_name = "2. Productos"
    #models = {"line",models.Line}
